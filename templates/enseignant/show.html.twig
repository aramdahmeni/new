{% extends 'base.html.twig' %}

{% block title %}Enseignant{% endblock %}

{% block body %}
    <h1>Enseignant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseignant.id }}</td>
            </tr>
            <tr>
                <th>TypeENS</th>
                <td>{{ enseignant.typeENS }}</td>
            </tr>
            <tr>
                <th>CodeENS</th>
                <td>{{ enseignant.codeENS }}</td>
            </tr>
            <tr>
                <th>NbAnneeExp</th>
                <td>{{ enseignant.nbAnneeExp }}</td>
            </tr>
            <tr>
                <th>Matiere</th>
                <td>{{ enseignant.matiere }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enseignant_index') }}">back to list</a>

    <a href="{{ path('app_enseignant_edit', {'id': enseignant.id}) }}">edit</a>

    {{ include('enseignant/_delete_form.html.twig') }}
{% endblock %}
